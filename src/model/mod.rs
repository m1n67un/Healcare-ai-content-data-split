pub enum RunType {
    SPLIT_INIT,
    DATA_SPLIT_TEST_DIRECTORY,
    DATA_SPLIT_TRAIN_DIRECTORY,
    DATA_SPLIT,
    DECAYED_COUNT
}

impl Default for RunType {
    fn default() -> Self {
        todo!()
    }
}

impl RunType {
    
}